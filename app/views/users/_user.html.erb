<li>
	<%= link_to user.name, user %> <%= user.email %> <%= user.role %> 
	<% if is_admin(current_user) %>
		| <%= link_to "delete", user, method: :delete, data: { confirm: "You sure?" } %>
	<% end %> 
</li>